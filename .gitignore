# dependencies
node_modules/
.pnpm-store/

# next.js
.next/
out/

# viewer package build output. Deliberately NOT committed: vendoring build
# artifacts is what lets the package and its consumers drift apart, which is the
# failure this package exists to prevent. Consumers build it instead.
packages/viewer/dist/

# env files
.env
.env.local
.env.*.local

# misc
.DS_Store
*.log
npm-debug.log*
tsconfig.tsbuildinfo

# vercel
.vercel

certificates
TODO.md
.handoffs/
.pi/
