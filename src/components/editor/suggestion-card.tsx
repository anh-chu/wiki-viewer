/**
 * Compatibility export. Pending suggestions now render through the
 * ProseMirror suggestion decorator and use SuggestionReviewPopover for review.
 */
export { SuggestionReviewPopover as SuggestionCard } from "./suggestion-review-popover";
